version: "2"

sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./internal/storage/queries"
    gen:
      go:
        package: "db"
        out: "./internal/storage/db"
        sql_package: "pgx/v5"